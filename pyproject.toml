[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "poker-solver"
version = "0.1.0"
requires-python = ">=3.9"

[project.scripts]
poker-solver = "poker_solver.solver:main"
